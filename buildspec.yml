version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
  build:
    commands:
      - echo "Building the static website..."
      - mkdir -p dist
      - cp index.html dist/
      - cp styles.css dist/
      - cp readme.md dist/

artifacts:
  files:
    - dist/**/*
