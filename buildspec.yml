version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
  build:
    commands:
      - echo "Building the static website..."


artifacts:
  files:
    - index.html
    - styles.css
    - readme.md
